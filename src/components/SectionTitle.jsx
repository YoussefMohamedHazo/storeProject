

const SectionTitle = ({text}) => {
  return (
    <div className=" border-b border-base-300">
      <h2 className=" tracking-widest font-medium text-3xl capitalize">{text}</h2>
    </div>
  )
}

export default SectionTitle